jQuery(function ($) {
    // $("[data-oso-super-cache-notice]").length && $(document).on("click", "[data-oso-super-cache-notice]", function () {
    //     $.ajax(ajaxurl, {
    //         type: "POST",
    //         data: {
    //             action: "oso_super_cache_handler",
    //             type: $(this)[0].dataset.osoSuperCacheNotice,
    //             _ajax_nonce: $(this)[0].dataset.osoSuperCacheNonce
    //         }
    //     })
    // }),
    // $(".oso-super-cache-toolbar-cache-maintenance").length && $(document).on("click", ".oso-super-cache-toolbar-cache-maintenance", function (a) {
    //     a.preventDefault(), confirm($(this).children("[data-oso-super-cache-confirm-msg]")[0].dataset.osoSuperCacheConfirmMsg) && $.ajax(ajaxurl, {
    //         type: "POST",
    //         data: {
    //             action: "oso_super_cache_handler",
    //             type: $(this).children("[data-oso-super-cache-confirm-msg]")[0].dataset.osoSuperCacheMaintenanceType,
    //             _ajax_nonce: $(this).children("[data-oso-super-cache-confirm-msg]")[0].dataset.osoSuperCacheNonce
    //         }
    //     }).done(function (a) {
    //         a = $.parseJSON(a), "undefined" !== a.message && alert(a.message)
    //     })
    // })
});